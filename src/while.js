var lineItemCount = 30;
var currentItem = 0;

// while currentItem is less than lineItemCount 
while (currentItem < lineItemCount) {
    console.log('item:' + currentItem);
    // increment current item
    currentItem++;
}